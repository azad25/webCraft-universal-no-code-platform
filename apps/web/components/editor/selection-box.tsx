'use client'

import { m } from 'framer-motion'

interface SelectionBoxProps {
  element: any
}

export function SelectionBox({ element }: SelectionBoxProps) {
  // This component can be extended to show resize handles
  // For now, it's handled by the widget-renderer
  return null
}
